/**
 * GymSelector component for Phase 2.2 (multi-gym support)
 *
 * Currently a placeholder. When Gym model is added to the schema:
 * - Fetch user's affiliated gyms via getMyGyms() API
 * - Render as dropdown/radio group
 * - Allow trainer to specify which gym client is trained at
 *
 * TODO: After gym schema is added to Prisma
 * - Add Gym model to schema.prisma
 * - Add Professional.gyms relation
 * - Create getMyGyms() API endpoint
 * - Enhance PublicLink to include gymId
 */

export function GymSelector() {
  return null; // Placeholder for Phase 2.2
}
